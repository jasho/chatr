namespace ChatR.Maui.App.Models;

/// <summary>A selectable AI chat backend shown in the AI Chat page's provider picker.</summary>
public sealed record AiChatProviderOption(string Key, string DisplayName);
