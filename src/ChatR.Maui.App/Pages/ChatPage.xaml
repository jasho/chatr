<?xml version="1.0" encoding="utf-8" ?>
<ContentPageBase
    x:TypeArguments="ChatPageViewModel"
    x:Class="ChatR.Maui.App.Pages.ChatPage"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:ChatR.Maui.App.Infrastructure.Converters"
    Title="Chat">

    <ContentPage.Resources>
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <Picker
            Grid.Row="0"
            Margin="8,8,8,0"
            Title="AI provider for @AI"
            ItemsSource="{Binding AiProviders}"
            ItemDisplayBinding="{Binding DisplayName}"
            SelectedItem="{Binding SelectedAiProvider}" />

        <Label
            Grid.Row="1"
            Text="{Binding ConnectionStatusText}"
            IsVisible="{Binding IsConnected, Converter={StaticResource InvertedBoolConverter}}"
            HorizontalOptions="Center"
            Padding="8,4"
            FontSize="12"
            TextColor="{AppThemeBinding Light=#888, Dark=#999}" />

        <CollectionView
            x:Name="MessagesCollectionView"
            Grid.Row="2"
            ItemsSource="{Binding Messages}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="ChatMessage">
                    <VerticalStackLayout Padding="12,6">
                        <Label
                            Text="{Binding Sender}"
                            FontAttributes="Bold"
                            FontSize="12"
                            TextColor="{AppThemeBinding Light=#555, Dark=#aaa}" />
                        <Label Text="{Binding Text}" FontSize="16" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Grid
            Grid.Row="3"
            ColumnDefinitions="*,Auto"
            Padding="8"
            ColumnSpacing="8">
            <Entry
                Grid.Column="0"
                Placeholder="Type a message... (mention @AI to ask the assistant)"
                IsEnabled="{Binding IsConnected}"
                Text="{Binding MessageText}"
                ReturnCommand="{Binding SendMessageCommand}" />
            <Button
                Grid.Column="1"
                Text="Send"
                Command="{Binding SendMessageCommand}" />
        </Grid>

    </Grid>

</ContentPageBase>
